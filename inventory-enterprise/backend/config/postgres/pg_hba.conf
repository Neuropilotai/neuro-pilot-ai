# PostgreSQL Client Authentication Configuration
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Replication connections
host    replication     replicator      172.26.0.0/16           md5
host    replication     replicator      0.0.0.0/0               md5

# Docker network connections
host    all             all             172.26.0.0/16           md5

# Application connections
host    inventory_enterprise    inventory_admin     0.0.0.0/0      md5

# Deny all others
host    all             all             0.0.0.0/0               reject

# Production security notes:
# 1. Replace 0.0.0.0/0 with specific application server IPs
# 2. Consider SSL/TLS (hostssl instead of host)
# 3. Use scram-sha-256 instead of md5 for better security
# 4. Restrict database access by tenant if needed
# 5. Enable SSL: ssl = on in postgresql.conf
