# Production (Railway) environment template
# Set these in Railway project variables. Do NOT commit real secrets.

# Auth
JWT_SECRET=your_prod_jwt_secret
OWNER_DEVICE_ID=your_owner_device_id

# Database (Railway Postgres or external)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional integrations
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
REORDER_WEBHOOK_URL=

# Misc
NODE_ENV=production
PORT=8080

